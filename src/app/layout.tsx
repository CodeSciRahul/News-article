import type { Metadata } from "next";
import {
  ClerkProvider,
  SignInButton,
  SignUpButton,
  SignedIn,
  SignedOut,
  UserButton,
} from "@clerk/nextjs";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Latest News Articles | Stay Updated with Breaking News",
  description:
    "Get the latest news articles on trending topics, breaking news, and in-depth analysis. Stay informed with up-to-date coverage on politics, technology, entertainment, and more.",
  keywords: [
    "news",
    "articles",
    "breaking news",
    "latest news",
    "trending news",
    "politics",
    "technology",
    "entertainment",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <ClerkProvider>
      <html lang="en">
        <body
          className={`${geistSans.variable} ${geistMono.variable} antialiased bg-slate-100`}
        >
          <header className="flex justify-end items-center p-4 gap-4 h-24 mb-16 bg-white shadow-sm">
            <SignedOut>
              <SignInButton />
              <SignUpButton />
            </SignedOut>
            <SignedIn>
              <UserButton />
            </SignedIn>
          </header>
          {children}
        </body>
      </html>
    </ClerkProvider>
  );
}
